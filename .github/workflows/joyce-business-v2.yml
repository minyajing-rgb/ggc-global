name: Joyce legacy V2 (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'V2 is archived. Use the Joyce coastal V3 website workflow. No PDF generation or old-theme rebuild is performed.'
